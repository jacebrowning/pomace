[pytest]

addopts =
    --strict
    --pdbcls=tests:Debugger

    -r sxX

    --cov=pomelo
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
