[pytest]

addopts =
    --strict
    --pdbcls=tests:Debugger

    -r sxX

    --cov=pomace
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
