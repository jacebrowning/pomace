[pytest]

addopts =
    --strict-markers

    -r sxX
    --show-capture=log

    --cov-report=html
    --cov-report=xml
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
